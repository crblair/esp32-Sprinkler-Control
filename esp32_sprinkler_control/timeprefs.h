#ifndef TIMEPREFS_H
#define TIMEPREFS_H
#include <Arduino.h>

// currentTimeZoneNameStr removed; handled by configTime() and system time.
// currentDstEnabled removed; handled by configTime() and system time.
// loadTimezoneFromPreferences removed; handled by configTime() and system time.
// saveTimezoneToPreferences removed; handled by configTime() and system time.
// loadDstFromPreferences removed; handled by configTime() and system time.
// saveDstToPreferences removed; handled by configTime() and system time.

#endif // TIMEPREFS_H
